name: App Build Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint
        # Si vous n'avez pas de commande lint définie, vous pouvez la commenter ou supprimer

      - name: Run tests
        run: npx jest --ci --passWithNoTests

      - name: Expo doctor
        run: npx expo-doctor

      - name: Build app preview (Expo)
        run: |
          npx expo install --check
          # Désactiver la génération des ressources
          npx expo prebuild --clean --no-install


  e2e-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Utiliser --no-install pour éviter la génération des ressources
      - name: Build iOS app
        run: npx expo prebuild --platform ios --no-install

  e2e-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Utiliser --no-install pour éviter la génération des ressources
      - name: Build Android app
        run: npx expo prebuild --platform android --no-install
