import { View } from 'react-native';
import colors from '../../constants/colors';

export default function SearchBarLine () {
    return (
        <View style={{
            width: 300,
            height: 1,
            backgroundColor: colors.SoftAnthracite,
            borderRadius: 5,
            opacity: 90,
            margin: 10,
        }}>

        </View>
    )
}
